{
  "name": "@unifyvault/api",
  "version": "1.0.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "test": "echo 'api: test'"
  },
  "devDependencies": {
    "@unifyvault/tsconfig": "workspace:*",
    "@unifyvault/eslint-config": "workspace:*"
  }
}
